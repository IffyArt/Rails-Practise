<div class="collapse navbar-collapse">
    <ul class="nav navbar-nav navbar-left">
      <% kinds.each do |kind| %>
        <li><%= link_to kind.name, product_kind_path(kind) %></li>
      <% end %>
    </ul>
</div>